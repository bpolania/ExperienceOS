"""Experimental, removable ExperienceOS extensions.

Nothing here is part of the deterministic kernel or the published Phase
14 surface. These modules are optional, shadow-only experiments that
reuse existing interfaces and can be deleted with no effect on the core
package. Keeping them outside ``experienceos/`` preserves the core's
provider-neutrality and its separation from optional learned paths.
"""
