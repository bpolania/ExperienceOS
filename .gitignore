# Local agent instructions (kept out of the repo)
CLAUDE.md

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/

# Local credentials (copy .env.example to .env; never commit)
.env

# Local demo databases (also covers runtime benchmark databases)
*.sqlite
*.sqlite3
.experienceos/

# Model weights: local GGUF files must never be committed
*.gguf

# Embedding model weights and caches (Phase 11): local-only assets in
# ONNX / safetensors / PyTorch formats must never be committed
*.onnx
*.safetensors
*.pt
*.pth

# Benchmark runtime artifacts. Small committed evidence lives in
# benchmarks/results/committed/, which is deliberately NOT ignored.
benchmarks/results/local/
benchmarks/.cache/
benchmarks/data/external/

# Tooling / IDE
.idea/
.brv/
.pytest_cache/
.DS_Store
