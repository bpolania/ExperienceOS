# Grounded Extraction Ablation Evidence

Frozen lifecycle runs for the grounded-extraction systems. Primary extraction aggregates are computed here on the `experienceos-lifecycle-v1` annotations; the external subset is classified separately. Development fixtures appear only as a smoke section and are never mixed into these aggregates.

Normalized result digest: `544421a1a6f580fcda5dc9c3a1b89e4ce71c0212eae8be2399859848c194e2e2`

Systems: canonical reference (grounded extraction disabled) and deterministic grounded extraction (benchmark-only adopted, isolated state). No controller is adopted; no default behavior changes.

Latency fields are excluded from the digest per the established convention.
